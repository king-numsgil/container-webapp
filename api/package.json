{
	"name": "api",
	"version": "0.1.0",
	"description": "",
	"license": "MIT",
	"scripts": {
		"build:app": "./node_modules/.bin/foal rmdir build/app && copy-cli \"src/**/*.html\" build && tsc -p tsconfig.app.json",
		"build:app:w": "tsc -p tsconfig.app.json -w",
		"start": "node ./build/index.js",
		"start:w": "supervisor -w ./build --no-restart-on error ./build/index.js",
		"develop": "yarn run build:app && concurrently \"yarn run build:app:w\" \"yarn run start:w\"",
		"build:scripts": "./node_modules/.bin/foal rmdir build/scripts && tsc -p tsconfig.scripts.json",
		"build:scripts:w": "tsc -p tsconfig.scripts.json -w",
		"build:migrations": "./node_modules/.bin/foal rmdir build/migrations && tsc -p tsconfig.migrations.json",
		"migration:generate": "./node_modules/.bin/typeorm migration:generate",
		"migration:run": "./node_modules/.bin/typeorm migration:run",
		"migration:revert": "./node_modules/.bin/typeorm migration:revert"
	},
	"engines": {
		"node": ">=8"
	},
	"dependencies": {
		"@foal/cli": "^1.12.0",
		"@foal/core": "^1.12.0",
		"@foal/typeorm": "^1.12.0",
		"pg": "^8.5.1",
		"source-map-support": "^0.5.19",
		"typeorm": "^0.2.29"
	},
	"devDependencies": {
		"@types/node": "^14.14.10",
		"concurrently": "^5.3.0",
		"copy": "^0.3.2",
		"supervisor": "^0.12.0",
		"typescript": "^4.1.2"
	}
}
